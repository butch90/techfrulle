# Babymac's Techfrulle project

## Introduction

> This project shows a simple way to create a chrome extension with react with webpack

## Installation

> Start with "npm install" to install the needed dependencies
Followed with either "npm start" to start develop mode or...
"npm run build" to run the webpack build sequence.